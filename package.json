{
  "name": "grace-and-force-ai",
  "version": "1.0.0",
  "description": "Voice AI Debate Platform",
  "scripts": {
    "start": "cd backend && node server.js",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && node backend/build-diplomat365-days.js && node backend/migrate-diplomat365.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "puppeteer": "^24.42.0",
    "ssh2": "^1.17.0"
  },
  "devDependencies": {
    "exceljs": "^4.4.0",
    "xlsx": "^0.18.5"
  }
}
